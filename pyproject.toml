[project]
name = "XenoCore"
version = "1.0.0"
description = "A GUI-based Vagrant manager made to manage the environments, plugins, and Vagrantfiles of your system."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dearpygui>=2.3.1",
    "jinja2>=3.1.6",
]
